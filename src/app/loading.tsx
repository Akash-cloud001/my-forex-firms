import LoadingScreen from '@/components/ui/LoadingScreen'
import React from 'react'

const LoadingPage = () => {
  return (
    <LoadingScreen title="Getting things ready..." subtitle="This will only take a moment." />
  )
}

export default LoadingPage